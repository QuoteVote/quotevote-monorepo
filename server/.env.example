# Server environment example
# Note: Default GraphQL server port is 3000 unless overridden
PORT={your_graphql_server_port}

# Client base URL (no trailing slash). Example for Vite dev server:
# http://localhost:8080
CLIENT_URL={your_client_url}

# Mongo connection string
DATABASE_URL=mongodb://{your_database_url}

# JWT secret used to sign access tokens
SECRET={your_secret_key}